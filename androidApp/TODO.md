# Android app — TODO

Done so far: home + bottom navigation, Room local cache (with real migrations), practice
progress in the TopAppBar, backend sync (offline-first), email/password + Google sign-in,
JWT access + refresh tokens with logout, front-of-card images, deck delete, and a searchable
+ sortable library.

Remaining / planned (see the root README "Status" for the cross-platform picture):

- [ ] Surface sync/offline state in the UI (e.g. last-synced, retry on failure)
